/*

  output is c:/program files (86x)...

*/

#include <iostream>
using namespace std;
struct DirectAXIS{

    long app = 0.0;
    double hadrine = 0.0;
    string _com;
    char dotcom = '0';
    int communicate[100000];
    char dotnet[100000];


};
void c_lease()
{
    DirectAXIS ajax;
    string connect;
    int comm_array[100000];
    char connect_api[100000];
    string outbit;

    for (int index = 0; index < 10000; ++index){
       ajax.app = ajax._com[index];
       ajax.dotcom = ajax.app;
       connect = ajax.dotcom;
       comm_array[index] = connect[index];
       connect_api[index] = comm_array[index];
       outbit = connect_api[index];
       cout << outbit;
       /*
       for (int index = 0; index < 12; ++index){
            ajax.communicate[index] = outbit[index];
            ajax.dotnet[index] = ajax.communicate[index];
            ajax.app = ajax.dotnet[index];
            ajax.hadrine = 0;

            ajax.hadrine = ajax.app;

            cout << ajax.hadrine << endl; */
       }



}
int main()
{
    c_lease();

    return 0;
}
